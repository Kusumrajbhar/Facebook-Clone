import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles({
  page: {
    background: "#eceff1",
  },
  leftPaper: {
    background: "#eceff1",
    overflow: "scroll",
    marginRight: "auto",
  },
  userInfo: {
    display: "flex",
    alignItems: "center",
    marginLeft: "15px",
  },
});

export default useStyles;
